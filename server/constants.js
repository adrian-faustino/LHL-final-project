const MAX_PLAYERS_PER_LOBBY = 4;

const VIEW_TIME = 1000; // fthe length of time in InstructionsView
const ROUND_TIME = 300000; // the length of time in DrawGameView

const constants = {
  MAX_PLAYERS_PER_LOBBY,
  VIEW_TIME,
  ROUND_TIME
}

module.exports = constants;