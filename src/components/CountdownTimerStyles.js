const timerStyles = (key, val) => {
  const styles = {

  }

  if(key) {
    styles[key] = val;
  }

  return styles;
}

const CountdownTimerStyles = {
  timerStyles
}

export default CountdownTimerStyles;